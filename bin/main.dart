import 'fonksiyon_mantik.dart';
import 'generate_dates.dart';
import 'generate_hashtags.dart';
import 'how_many_times.dart';
import 'number_format.dart';
import 'number_format_twitter.dart';
import 'question_algorythm.dart';
import 'simplify.dart';

/**
 * Deneme
 * * Highlighted
 * ! Bi Kodist
 * ? Maybe baby :)
 * TODO: to do :P
 */

////
void main() async {
  // rastgeleSozGetir();

  // sumOfNumbers(10);

  // ardisikSayilarToplami();

  // tamSayiBolenSayisi(-20);

  // ebobEkok(4, 10);

  // simplifyMainFunc();

  // hicridenMiladiye(1360);
  // miladidenHicriye(1941);

  //int createdPassword = createCustomPassword();
  //isCustomPassword(createdPassword);

  // tarih(DateTime(2020, 1, 1, 0, 0, 0));

  //islemYap1(5, "3/2");
  //islemYap2(84, "1/4", true); // 5 * 5/2
  //islemYap2(84, "1/4", false); // 3 * 1/3

  //bmiHesapla(kilo: 80, boy: 180);

  //print(gecenZamanFormat(DateTime.parse("2020-06-24 11:07:19.607542")));

  //for(int i = 0; i < 10; i++) {
  //  print(gecenZamanFormat(mockDate(DateTime.now().subtract(Duration(hours: 2))), kisa: true));
  //}

  // fakeAPI();

  // generateDatesMainFunc();

  // generateChainMainFunc();

  // weeklyPlannerMainFunc();

  // numberFormatMainFunc();

  // numberFormatTwitterMainFunc();

  // findExamResultsMainFunc();

  // howManyTimesMainFunc();

  // questionAlgorythmMain();

  // fonksiyonMantikMain();
}
